name: Email Step Action

on:
  workflow_dispatch:  # Manual trigger

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 21f3002611@ds.study.iitm.ac.in
        run: echo "Hello, world!"
